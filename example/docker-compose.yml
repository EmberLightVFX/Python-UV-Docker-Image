services:
  python-app:
    build: ..
    ports:
      - "8080:8080"
    volumes:
      - ./app:/app
    environment:
      - PYTHONUNBUFFERED=1
      # Optional: Set custom user and group IDs to match host user
      # - PUID=1000
      # - PGID=1000
